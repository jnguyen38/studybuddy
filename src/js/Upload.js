import React from "react";

export default function Upload() {
    return (
        <div className={"upload-container"}>
            <div className={"upload-header"}>
                <h1 style={{textAlign: 'left'}}>Upload a new study space</h1>
            </div>
        </div>
    );
};